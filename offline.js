(function () { "use strict"; document.addEventListener("DOMContentLoaded", function () { var button = document.getElementById("retryConnection"); var status = document.getElementById("connectionStatus"); if (!button || !status) return; function retry() { if (navigator.onLine) { status.textContent = "Connection restored. Reloading..."; window.location.reload(); } else { status.textContent = "Still offline. Your saved pages remain available."; } } button.addEventListener("click", retry); window.addEventListener("online", retry); }); })();
