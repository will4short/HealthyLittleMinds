(function () {
  "use strict";

  // Copy this file to supabase-config.js during environment setup. Only the
  // project URL and publishable browser key belong here. Never add a Supabase
  // secret/service key, Payhip secret, Resend key, or Google client secret.
  window.HLM_SUPABASE_CONFIG = Object.freeze({
    url: "https://YOUR_PROJECT_REF.supabase.co",
    publishableKey: "YOUR_SUPABASE_PUBLISHABLE_KEY"
  });
})();
