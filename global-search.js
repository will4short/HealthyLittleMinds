(function () { "use strict"; document.addEventListener("DOMContentLoaded", function () { var input = document.getElementById("globalSearch"); var cards = Array.prototype.slice.call(document.querySelectorAll("[data-search-card]")); var status = document.getElementById("searchStatus"); if (!input) return; function update() { var filter = input.value.trim().toLowerCase(); var count = 0; cards.forEach(function (card) { var match = !filter || card.textContent.toLowerCase().indexOf(filter) >= 0; card.classList.toggle("is-hidden", !match); if (match) count += 1; }); if (status) status.textContent = filter ? count + " result" + (count === 1 ? "" : "s") + " found." : "Showing all resources."; } input.addEventListener("input", update); update(); }); })();
